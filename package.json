{
  "name": "nodejs-with-typescript-template",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "yarn run build && node build/index.js",
    "start:dev": "npx nodemon"
  },
  "dependencies": {
    "@types/node": "^20.5.7",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.1",
    "typescript": "^5.2.2"
  }
}
